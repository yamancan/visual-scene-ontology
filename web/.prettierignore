# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Markdown is managed separately (docs are owned/edited by hand, not
# code-formatted), so keep it out of the prettier lint gate.
*.md
